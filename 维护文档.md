# Balance HUD 维护文档

## 项目概览与目标

Claude Code 全功能插件：实时 HUD 状态栏（上下文健康度/推理力度/缓存效果/用量监控/工具追踪/Agent 状态/会话信息）+ DeepSeek API 余额监控。基于 [claude-hud](https://github.com/jarrodwatts/claude-hud) v0.6.0 (MIT) 改造，与 balance-hud (余额监控) 整合为独立插件。**边界**：HUD 状态栏与 API 无关（任意 API 可用）；仅 DeepSeek 余额行依赖 DeepSeek API，用其他 API 时该行自动隐藏。

## 维护状态

个人维护项目，按需更新，PR 欢迎但不保证及时。`statusline.mjs`（源码入口）→ 构建进 `dist/`（`main` 指 `dist/index.js`）。**改源码务必重新构建 dist**，否则 `npm start` / 插件加载的还是旧产物。

## 构建 / 运行 / 测试

- **运行**：Claude Code 终端配置 statusLine 指向本插件；`/balance-hud:setup` 一键接入
- **测试**：4 个斜杠命令（`/balance-hud:setup` :configure :debug :language）实测需在 Windows + Git Bash 下手动过一遍；当前无自动化测试
- **构建**：Node ≥18，ESM（`type: module`）——改 `statusline.mjs` 后跑脚本产出 `dist/` 下各模块（i18n / render / utils）
- **依赖**：无需安装第三方运行时依赖（`package.json` 无 dependencies）

## 目录结构

| 路径 | 职责 |
|------|------|
| `statusline.mjs` | 状态栏源码入口（改它 + 重构构建） |
| `dist/` | **构建产物**：`index.js`（main）+ i18n/render/utils 子模块 |
| `package.json` | 元数据：name / version / main / type |
| `config.json` | 插件默认配置（多语言等） |
| `commands/` | 斜杠命令定义/脚本（:setup :configure :debug :language） |
| `hooks/` | 会话钩子（事件 → 状态栏刷新） |
| `scripts/` | 构建 / 发布辅助脚本 |
| `.claude-plugin/` | 插件注册元数据 |
| `NOTICE.md` | 基于 claude-hud 的版权声明 |
| `README.md` + `LICENSE` | 双语说明 / MIT |

## 发布流程（对齐 improve skill）

1. 改版本号：version 在**三处**——`package.json` `version`、README 标题 `vX.Y.Z`、底部徽章 —— **改版本必须 `grep X.Y.Z` 全改**（从 v1.1.3 起余额监控模块独立版本，注意正文里旧版本引用），改完复查零残留
2. README「变更日志」节顶部加新版本段（分类词：新增/修复/删除/优化/版本），中英 README 同步
3. 全局 grep 旧版本零残留；确认发布资产无本机个性化信息（路径/key）
4. commit（作者 `226671264+BFRKQSB7@users.noreply.github.com`）→ tag `v{版本号}` → push → `gh release create`

## 提交规范

Conventional Commits 轻量 5 前缀：`feat` / `fix` / `docs` / `refactor` / `chore`。小更新+距上次不久可覆盖发布（重打同 tag）。

## 版本纪律

semver：破坏性→x+1、新功能→y+1、修 bug→z+1。**tag 必须与 `package.json` + README 版本同步**（历史上出现过 tag 滞后：v2.2.2 发布时 tag 晚补）。历史 tag：v1.0.0 ~ v2.2.2。

## 本仓库特有坑 / 注意

- **改 statusline.mjs 不重建 dist = 改了等于没改**（运行时加载 dist）
- 上游 claude-hud 更新时可对照合入，但**外部引用保持 NOTICE/README 署名**（MIT 义务）
- "与 API 无关"是卖点，改动别破坏非 DeepSeek API 下的纯净状态栏
- 本仓库徽章历史格式为**无 v 前缀**（`version-2.2.2-blue`）；新版本沿用，保持与前缀仓库（如 kakuyomu `vX.Y.Z`）区分，改版本时别弄混